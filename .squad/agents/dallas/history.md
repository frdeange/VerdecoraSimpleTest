# Dallas — History

## Learnings

- **2026-05-08:** Joined as DevOps for Verdecora Simple project. Key shift: moving from self-hosted ACA runners (inside private VNet) to standard GitHub-hosted runners. Need to redesign all CI/CD workflows. User is Kiko de Angel.
- **2026-05-08:** Issue #4 redesign standardised CI/CD on GitHub-hosted Ubuntu runners and Azure OIDC (`azure/login@v2` with repo variables). `build-deploy.yml` is now a four-stage pipeline: validate → deploy-infra → build-and-push → deploy-apps. Squad automation workflows were reviewed and already compliant with `ubuntu-latest`.
- **2026-05-08 14:00:** Completed Issue #4 — redesigned 4 CI/CD workflows (build-deploy, ci, bicep-validate, upload-web-ci) for ubuntu-latest runners. Configured GitHub OIDC federation with Azure. Replaced long-lived secrets with short-lived token auth. PR #9 created. Successfully unblocked by Parker's Issue #1 (private-network module deletions), no self-hosted runner infrastructure dependency remains. OIDC federation now foundational for Parker's subsequent PaaS public-access configuration.
- **2026-05-08 15:00:** Parker deployment to rg-verdecora-simple-dev complete. Infrastructure ready for CI/CD. ACR: `acrvdsdev4vtapr.azurecr.io`; Cosmos: `cosmos-vds-dev-4vtapr.documents.azure.com`; KV: `kv-vds-dev-4vtapr.vault.azure.net`. Configure repository variables (`AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`, `RESOURCE_GROUP`, `ACR_NAME`, `ENVIRONMENT`) and trigger build-deploy workflow to push container images.
- **2026-05-08 16:30:** Added `scripts/setup-github-oidc.ps1` plus `scripts/README.md` to bootstrap GitHub OIDC end-to-end. The script validates `az`/`gh`, auto-discovers the active subscription and ACR in the supplied resource group, creates or reuses the Entra app + service principal + federated credentials, assigns `Contributor` and `AcrPush`, and sets the required GitHub repo variables. Verified against `frdeange/VerdecoraSimpleTest` and `rg-verdecora-simple-dev`; current client ID is `a9a77855-4408-463a-8eb9-e1ed217c6a95`.
- **2026-05-08 16:45:** Fixed OIDC RBAC scope mismatch after `build-deploy.yml` failed on subscription deployment validation. Assigned `Contributor` to service principal `a9a77855-4408-463a-8eb9-e1ed217c6a95` at subscription `/subscriptions/0acbc8a1-0f3e-498e-b86b-6fa5468730e2`, updated `scripts/setup-github-oidc.ps1` to grant subscription-scope `Contributor` for `targetScope = 'subscription'` Bicep deployments, and re-ran Actions run `25561335228`.
- **2026-05-08 16:55:** Reworked GitHub Actions into a smart split pipeline. `build-deploy.yml` now uses `dorny/paths-filter@v3` on `ubuntu-latest` to detect service-level changes and only build/deploy touched workloads via Azure OIDC. Infrastructure deployment moved into a dedicated `deploy-infra.yml` that triggers only for `infra/**` changes or manual dispatch, preventing infra redeploys on every code push.
- **2026-05-08 18:35:** Batched infra PRs #22 and #24 onto `squad/infra-batch-22-24` so `deploy-infra.yml` runs once. Merged the conditional ACR registry fix with the HITL review queue IaC change, regenerated `infra/modules/main.json`, and revalidated with `az bicep build --file infra/modules/main.bicep`.
- **2026-05-08 22:40:** Fixed `build-deploy.yml` deploy jobs so first real image rollout configures ACR auth on Container Apps and Jobs via `--registry-server` + `--registry-identity system`. Added existence guards for orchestrator, HITL, dedup, escalation, and upload-web so deploy steps warn and skip cleanly when `enableContainerAppWorkloads=false` left those resources undeployed.
- **2026-05-08 22:55:** Corrected `build-deploy.yml` for repeat deployments: `az containerapp update` and `az containerapp job update` do not accept direct registry flags, so all deploy steps now run `az containerapp registry set` / `az containerapp job registry set` before updating the image. Verified the CLI contract with `az ... -h` and reran `python -m pytest tests/unit/test_upload_web_scaffold.py`.
- **2026-05-09 08:00:** Architected GitHub OIDC bootstrap automation (`scripts/setup-github-oidc.ps1`). Auto-discovers active subscription and single ACR in target resource group; creates/reuses Entra app registration and service principal; assigns `Contributor` at subscription scope (required for `targetScope='subscription'` Bicep validation) and `AcrPush` scoped to ACR. Generates GitHub repository variables: `AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`, `RESOURCE_GROUP`, `ACR_NAME`, `ENVIRONMENT`. Verified end-to-end for `frdeange/VerdecoraSimpleTest` and `rg-verdecora-simple-dev`; principal `a9a77855-4408-463a-8eb9-e1ed217c6a95` now active.
- **2026-05-09 09:00:** Implemented smart path-filtered CI/CD split. `deploy-infra.yml` dedicated to infrastructure (`infra/**` or manual dispatch). `build-deploy.yml` redesigned with `dorny/paths-filter@v3` to trigger only on application changes (`src/**`, `docker/**`, `pyproject.toml`, workflow) and manually; each service build + deploy is conditionally gated by path detection. Reduces unnecessary ACR builds and ACA rollouts; keeps infrastructure changes isolated from application churn.

**Cross-team context:**
- Ripley's orchestrator diagnostics (2026-05-09 P0) revealed upload-web missing `azure-servicebus` dependency and orchestrator running quickstart image. Dallas' CI/CD safeguard improvements now ensure: (1) all `az containerapp update` calls specify `--image` to prevent quickstart drift, (2) all image deploys configure `--registry-server` with system identity before pushing, (3) `build-deploy.yml` triggers precisely on orchestrator changes, rebuild + redeploy, and test.
- Burke's BC tool fix (PR #85) and Kane's HITL consumer (PR #84) will both benefit from Dallas' refined path-filtering — any edit to `src/mcp/bc_mcp/**` or `src/services/orchestrator/**` triggers automated rebuild + test.
- Dallas' OIDC bootstrap script is prerequisite for team self-service AKS deployments if Verdecora scales to multi-environment.

# Ash — History

## Learnings

- **2026-05-08:** Joined as Frontend Dev for Verdecora Simple project. Two web UIs to manage: upload-web (document upload interface) and HITL webform (human review interface). Front Door removal will affect how these are exposed. User is Kiko de Angel.
- **2026-05-08:** Updated upload-web auth redirect handling to support direct ACA public hosts via `UPLOAD_WEB_PUBLIC_ORIGIN`. Login/logout flows now stay relative by default for backward compatibility, but can emit absolute Easy Auth redirect targets when a public host must be explicit. HITL callback fallback now reads `HITL_WEBFORM_BASE_URL`, and Blob CORS/auth deployment docs were updated to point at ACA FQDNs instead of Front Door.
- **2026-05-08 14:20:** Issue #5 completed: updated upload-web CORS, auth endpoints, and model service configurations for public ACA deployment. Easy Auth flow now handles public ACA direct hosts, blob CORS deployment references ACA FQDNs, and HITL callback resolves via `HITL_WEBFORM_BASE_URL`. PR #12 created and merged. Frontend now aligned with public PaaS infrastructure.
- **2026-05-09 01:35:** Reworked upload-web preflight UX to render a formatted analysis card client-side instead of dumping API JSON. The upload stepper now advances to “Análisis”, shows supplier/date/albarán/store/confidence with warning states, and exposes a confirm CTA that POSTs to the existing confirm endpoint before redirecting to processing status.
- **2026-05-09 01:29:** Fixed the Mis albaranes status filters so they use canonical full-page navigation via /mis-albaranes?status=..., preserve the table layout for every status, keep active filters highlighted with the green Verdecora pill style, and redirect the legacy /mis-albaranes/filter endpoint to the canonical query-string route.

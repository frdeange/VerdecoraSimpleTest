# Parker — History

## Learnings

- **2026-05-08:** Joined as IaC Expert for Verdecora Simple project. Main task: strip private networking from Bicep modules while preserving all functional Azure resources. Current infra has ~27 Bicep modules. About 5-6 modules need removal and main.bicep needs significant refactoring. User is Kiko de Angel.
- **2026-05-08:** Issue #1 removed private-network composition from `infra/modules/main.bicep` and deleted the dedicated network/private-endpoint/NAT/runner/Front Door modules. `identity.bicep` has no runner-specific principal inputs, so no safe cleanup was needed there; subnet parameters remain only inside child modules until Issue #3 removes them.
- **2026-05-08:** To keep `main.bicep` buildable after removing subnet arguments, `container-apps.bicep` and `upload-web-app.bicep` now treat `infrastructureSubnetId` as optional and only emit `vnetConfiguration` when a subnet id is supplied. This preserves Issue #1 validation without changing the surviving module behavior when a subnet is still passed.
- **2026-05-08:** For Issue #2, the minimal public-access change set was enough: flip `publicNetworkAccess`/`publicNetworkAccessFor*` to `Enabled` and switch only explicit firewall defaults from `Deny` to `Allow`, while preserving RBAC/managed identity hardening (`disableLocalAuth`, `enableRbacAuthorization`, HTTPS-only, TLS 1.2, private blob access, model deployments, and diagnostics).
- **2026-05-08 14:00:** Completed Issue #1 — deleted 5 private-network Bicep modules (network, private-endpoints, nat-gateway, runners, frontdoor) and github-runner Dockerfile. Cleaned main.bicep. PR #8 created. This unblocks Dallas's CI/CD redesign (Issue #4) by removing self-hosted runner infrastructure dependency.
- **2026-05-08 14:19:** Issue #3 simplified ACA environments by removing subnet requirements and managed-environment VNet configuration from `container-apps.bicep` and `upload-web-app.bicep`, while keeping workload identities, ACR pulls, ingress, jobs, KEDA rules, and env vars intact. `main.bicep` no longer passes ACA subnet IDs or blocks `containerApps` on NAT/private endpoints, and resource group naming was aligned to `rg-verdecora-simple-${environment}`.
- **2026-05-08 15:05:** Issue #6 deployed the simplified infra to `rg-verdecora-simple-dev` in `swedencentral` after fixing three deployment blockers: (1) globally-conflicting resource names now use deterministic `verdecora-simple` suffixes, (2) the workspace-based App Insights alert query now uses `AppMetrics.Sum`, and (3) AI Foundry model deployments plus ACA workloads are disabled by default for infra-only rollouts because this subscription blocked `gpt-5` policy validation and the ACR had no workload images yet. Successful deployment: `verdecora-simple-deploy-20260508-150524`.
- **2026-05-08 14:19:** Issue #2 enabled public network access on 7 Bicep modules (storage, keyvault, cosmos, openai, search, container-registry, appinsights) by flipping `publicNetworkAccess` to `Enabled` and switching firewall defaults from `Deny` to `Allow`, while preserving all security hardening (RBAC, TLS 1.2, HTTPS-only, diagnostics). PR #10 created and merged.
- **2026-05-08 14:19:** Issue #3 completed: removed VNet integration from ACA by making `infrastructureSubnetId` optional in both `container-apps.bicep` and `upload-web-app.bicep`, eliminating subnet coupling while preserving workload identities, ACR authentication, KEDA job triggers, and environment variable injection. PR #11 created and merged. Master now has simplified ACA deployment model.
- **2026-05-08 16:55:** Issue #11 updated all remaining ACA workload default images to `mcr.microsoft.com/k8se/quickstart:latest` in `upload-web-app.bicep` and `container-apps.bicep`, matching the post-MVP jobs. This keeps infra-only ACA deployments from failing before the first ACR build publishes service images.
- **2026-05-08 17:35:** Issue #20 fixed all reported Bicep warnings: conditional principal-id outputs in `container-apps.bicep` now use null-safe access, `storage.bicep` child resources now use `parent` syntax and no longer assign the read-only `immutabilityPolicy`, and `monitoring.bicep` keeps the runtime-valid Log Analytics `sku` behind an inline BCP187 suppression while using the schema-correct `DisableLocalAuth` property. `az bicep build --file infra/modules/main.bicep` now completes cleanly aside from the Azure CLI's standalone upgrade notice.
- **2026-05-08 18:10:** Issue #22 made ACR registry wiring conditional in `upload-web-app.bicep` and `container-apps.bicep`. ACA apps/jobs now omit `configuration.registries` when all image params are empty and the MCR quickstart placeholder is in use, preventing first-deploy ACA resources from hanging in `InProgress` while trying to authenticate against the private ACR for public images. Revalidated with `az bicep build --file infra/modules/main.bicep` and cleaned up the stuck `verdecora-upload-web-dev` app plus the `acae-upload-web-dev` environment.
- **2026-05-08 18:30:** Issue #23 added the `hitl-review` Service Bus queue to `infra/modules/servicebus.bicep` with explicit delivery/TTL settings, passed the queue output through `infra/modules/main.bicep`, injected `HITL_QUEUE_NAME` into the orchestrator in `infra/modules/container-apps.bicep`, and revalidated with `az bicep build --file infra/modules/main.bicep` (0 errors).
- **2026-05-08 22:38:** Combined PR #26's build-deploy ACR-auth workflow fixes into `squad/infra-batch-26-workloads` and corrected the inherited infra toggle mistake so `enableWorkloads` / `enableContainerAppWorkloads` now default to `true`. This ensures orchestrator, HITL, dedup, and escalation deploy with infra, relying on the existing `mcr.microsoft.com/k8se/quickstart:latest` placeholder images for first-time rollouts. Revalidated with `az bicep build --file infra/modules/main.bicep`.
- **2026-05-08 23:29:** Issue #31 configured Easy Auth for `verdecora-upload-web-dev` using a new Entra app registration (`95188fb8-625a-4b83-a588-67d4ee801ddd`), updated the live ACA auth provider, and folded the `authConfigs` resource plus secure client-secret wiring into `infra/modules/upload-web-app.bicep` so upload-web auth is reproducible from the same module. Verified `/.auth/login/aad` now returns `302` to Microsoft login, revalidated `az bicep build --file infra/modules/main.bicep`, and opened PR #32.
- **2026-05-08 23:49:** Issue #33 fixed upload-web CSP for direct Blob uploads by centralizing CSP/header construction in `src/upload_web/middleware/security_headers.py`, normalizing `STORAGE_ACCOUNT_URL`/`BLOB_ACCOUNT` down to the storage origin, and wiring `SessionSecurityMiddleware` to use the same dynamic builder so live upload-web responses allow `connect-src 'self' https://stvdsdev4vtapr.blob.core.windows.net`. Added unit coverage for both middleware paths and URL normalization.
- **2026-05-09 00:27:** Issue #45 aligned Cosmos infra/runtime defaults by adding the `processing-records` container to `infra/modules/cosmos.bicep` under `albaranes-db`, injecting `DATABASE_NAME=albaranes-db` plus `PROCESSING_CONTAINER_NAME=processing-records` into orchestrator/HITL/escalation workloads, and setting `DATABASE_NAME` + `COSMOS_DATABASE` on `upload-web-app.bicep`. Revalidated with `az bicep build --file infra/modules/main.bicep`.
- **2026-05-09 01:20:** Issue #37 stopped Blob CORS from being wiped on infra redeploys by keeping the `blobServices` CORS rule in `infra/modules/storage.bicep`, expanding it to cover browser preflight (`OPTIONS`), and making `infra/modules/main.bicep` fall back to a temporary wildcard origin whenever `enableUploadWeb=true` but no explicit `uploadWebBlobCorsAllowedOrigins` are passed. Documented the temporary fallback in `infra/README.md` and revalidated with `az bicep build --file infra/modules/main.bicep`.
